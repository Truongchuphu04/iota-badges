# Example environment variables for the IOTA badges example
# Use a testnet node (Shimmer testnet example)
NODE_URL=https://api.testnet.shimmer.network
# Indexation key for messages (use a short string identifying your app)
INDEX=iota-badge
# JSON string or text payload to store in the message
MESSAGE={"badge":"Example Badge","level":"gold"}
